## URD-033 — The Scholar achievement's top tier is unreachable in a playthrough, the same bug URD-004 just fixed elsewhere
attempts: 0
files: src/data/achievements.ts
definition of done: `ACHIEVEMENTS`'s `scholar` entry (`src/data/achievements.ts:34-41`)
  gates its top tier at 10,000 total XP. The real course total (sum of
  every lesson's `.xp` across `ALL_LESSONS`) is ~7,220 XP — the top tier
  needs 38.5% more than a single honest playthrough provides, the identical
  bug class URD-004 fixed for `levelTitle`'s "Master" tier, in a sibling
  XP-gated system that item was never scoped to touch. Re-space
  `scholar`'s tiers (or the other achievements' tiers — `first-steps`,
  `wordsmith`, `calligrapher`, `flame-keeper`, `perfect-*` etc. were not
  individually re-checked when this was found; only `scholar` is confirmed
  broken) so every tier is reachable within the course's real totals for
  its metric, following URD-004's pattern: derive both numbers from real
  content in a test rather than hardcoding either, so this doesn't go
  stale the same way `levelTitle`'s thresholds did (twice).
verify: a test in src/lib or src/data asserting every achievement's top
  tier is reachable against the real course total for its metric
  (`totalXp` from `ALL_LESSONS[].xp`, `lessonsCompleted` from
  `ALL_LESSONS.length`, `wordsLearned` from `WORDS.length`, etc. — `streak`
  is not course-bound and may need its own reasoning, not a course-total
  comparison).
notes: Found by THE CRITIC reviewing URD-004 (commit b28856a), specifically
  while checking whether anything else in the app assumed level/XP
  thresholds that fix didn't touch. Not blocking that item — different
  file, different subsystem, URD-004 was narrowly scoped to `levelTitle`
  — but real: a badge nobody can earn on one playthrough, discovered by
  the exact question ("is the top tier reachable?") this project just
  learned to ask. THE CRITIC's own caveat: `finishLesson` grants XP on
  every replay (no once-only guard), so no tier is literally impossible,
  only unreachable in one honest playthrough — the right target to design
  against, matching how URD-004 itself was framed.

  RESOLVED: re-spaced `scholar`'s tiers from `[100, 500, 2000, 10000]` to
  `[50, 250, 1000, 5000]` — the same ~5x/~4x/~5x relative spacing as
  before, scaled so the top tier lands at ~69% of the real course total
  (7220 XP) rather than 138% of it, comfortable margin below the ceiling
  rather than landing on it exactly, matching `levelTitle`'s own ~76%
  precedent from URD-004.

  Checked all five course-bound metrics, not just `scholar` (the item's
  own honesty that "the others were not individually re-checked"):
  `lessonsCompleted` (top tier 40 vs real ceiling 348), `wordsLearned`
  (100 vs 2281, `WORDS.length`), `perfectLessons` (50 vs 348) all sit
  comfortably under their real ceilings and needed no change.
  `lettersLearned`'s top tier (40) sits at exactly 100% of its ceiling
  (`LETTERS.length` = 40, no margin) — traced directly rather than
  assumed: every one of the 40 letters is taught by some `letters`-kind
  lesson's `letterIds`, and the exercise generator's `letters`-kind
  branch unconditionally produces a graded (`type: 'letter'`) exercise
  for every letter across its `SIGHTINGS_PER_LETTER` rounds, so all 40
  are genuinely reachable in one playthrough — matching
  `gamification.test.ts`'s own precedent of asserting
  `toBeLessThanOrEqual` rather than strict `<`. `streak` is deliberately
  excluded from the course-bound comparison — not course-bound at all
  (a learner extends it by continuing to show up, uncapped by anything
  in `useProgressStore.ts`), matching the item's own framing.

  Added `src/data/achievements.test.ts`: for every `ACHIEVEMENTS` entry
  whose metric is course-bound, asserts its top tier fits within a
  ceiling derived from real content (`ALL_LESSONS`, `WORDS`, `LETTERS`),
  not hardcoded — plus a dedicated assertion that `scholar`'s top tier
  specifically sits under 85% of the course total, not just technically
  under 100% of it (the same "landing on it exactly" trap `levelTitle`
  was fixed away from).

  THE CRITIC (mandatory) dispatched — not CURRICULUM CRITIC, DESIGN
  CRITIC, or PLAYER, matching URD-004's own precedent for the identical
  shape of fix (no lesson, word, screen, or exercise-behavior content
  changed). Found no BLOCKING, no MAJOR. Two MINOR, neither actioned:
  (1) the test's metric→ceiling map has no exhaustiveness guard — a
  future new course-bound metric added to `AchievementDef['metric']`
  without a matching `ceilingFor` entry would silently pass through
  unchecked forever, the same shape of risk as `streak`'s deliberate
  skip, just generalized to content that doesn't exist yet; (2) a naming
  collision between this item's new `src/data/achievements.test.ts` and
  the pre-existing, unrelated `src/lib/achievements.test.ts` (DAILY_GOALS
  pace check) — confirmed not a bug (the pace-drift comment in
  `achievements.ts` correctly points at the other file), but a real trap
  for a future `git grep`. THE CRITIC independently reproduced every
  claimed number from scratch (course XP total, `ALL_LESSONS.length`,
  `WORDS.length`, `LETTERS.length`, all matching exactly), independently
  traced the letters-teaching/grading claim through the generator code
  directly rather than trusting a one-off script, confirmed scope
  (exactly two files touched, no UI hardcodes the old numbers,
  `AchievementsScreen.tsx` reads `tiers` dynamically), and independently
  reproduced the induced-failure test (reverting to the old tiers
  reproduces the two clear test failures, restoring reconfirms clean).

  `npx tsc --noEmit`: clean. `npx vitest run`: 177/177 passing (up from
  171 — 6 new tests). `npm run format:check`: clean.
