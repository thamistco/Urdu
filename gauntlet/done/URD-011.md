## URD-011 — Stop check:shape recomputing the same lesson
attempts: 0
files: scripts/check-shape.js, src/data/units.ts
definition of done: `buildLessonExercises` is called 5,070 times per run of
  check:shape, taking 2.5 to 3.4 seconds; memoised on lesson id and track it is
  about 700. Also `lesson.size` is wrong by exactly 3 on all 233 vocabulary
  lessons — `coverTopics` budgets `3n+4` and the generator emits `3n+1`. Nothing
  reads it for vocabulary lessons today, which is the only reason it is minor.
  Fix the arithmetic and memoise, with check:shape still exiting the same way.
verify: npm run check:shape
notes: Both found by THE CRITIC on URD-A02 as MINOR. Paired because they are the
  same file and the same sitting. Do not memoise across tracks by accident — the
  tracks emit different exercises for the same lesson and that difference is
  what the run and share rules exist to see.

  RESOLVED: added a memoising wrapper around `buildLessonExercises` inside
  `check-shape.js`, keyed on lesson id, track and the due-queue refs (track
  kept separate on purpose, per this item's own warning — verified the two
  tracks still cache as distinct entries). Measured on today's larger
  course: 5,839 total requests collapse to 999 real generations. Confirmed
  via `git stash`/`git stash pop` A/B comparison that `check:shape`'s
  output is byte-identical before and after — this item's own "still
  exiting the same way" requirement, verified exactly rather than
  approximately. The generator is not pure (`Math.random()`/`shuffle()`
  affect option order and, for some kinds, which exercise kind is asked),
  so memoising also fixes a latent inconsistency: every rule in the file
  now examines the same generated instance for a given (lesson, refs,
  track), rather than silently drawing independent random samples under
  the same nominal scenario.

  Fixed `CLOSING_EXERCISES` in `units.ts` from `4` to `1` — the constant
  confused "the matching board holds four words" with "the board is four
  array entries," when it is exactly one `{kind:'matching'}` exercise.
  After the fix, all 233 real vocabulary lessons' recorded `size` exactly
  matches what the generator actually emits (was 0 of 233, off by exactly
  3 each, before). Confirmed via repo-wide grep that nothing reads a real
  vocabulary lesson's `.size` at runtime, so this is a pure bookkeeping
  correction with zero learner-facing effect.

  THE CRITIC passed with no BLOCKING/MAJOR and one MINOR, fixed same round:
  `CLOSING_EXERCISES = 1` assumes the matching board always renders
  (`board.length === 4`), which holds for every real topic today (smallest
  has 11 words) but isn't structurally guaranteed — a future topic too
  small or homogeneous to fill the board would make the constant wrong by
  one again. Zero runtime impact either way, so not gating; fixed by
  extending the constant's own comment to state the assumption plainly.
  THE CRITIC independently reproduced every other claim from scratch
  (cache-key collision safety, track separation, the "nothing reads it"
  grep, the 233/233 arithmetic match, the 5,839→999 call reduction, and a
  full `check:all` run to completion — 26/26) rather than trusting the
  lead's report.
