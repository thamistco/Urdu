## URD-039 — A review's fallback content for a mastered unit never rotates, ever
attempts: 0
files: src/lib/review.ts
definition of done: `prioritizedPool`'s per-tier shuffle seed
  (`` `${lessonId}:words:${i}` `` / `` `${lessonId}:letters:${i}` ``) has no
  source of variation across replays of the same review by the same learner
  — same lesson, same `known` set, byte-identical output, every time. Once a
  learner has graded every word in a unit (the common case for a unit small
  enough that a fresh review of it is even reachable), the fallback always
  slices off the same fixed subset of that unit's words and never surfaces
  the rest. Give repeated reviews of the same unit some source of variation
  once the learner already knows everything in scope — a rotation keyed on
  something that actually changes between visits (visit count, a stored
  per-review cursor, or similar), not a a value fixed by `lessonId` alone.
verify: a test seeding a review's `known` set to the closing unit's full
  word list, calling the pool-selection twice with state representing two
  different real visits (however "visit" ends up being modeled), and
  asserting the two calls' chosen words differ when the unit has more words
  than the review's word-slot count.
notes: Found independently by both THE CRITIC and CURRICULUM CRITIC
  reviewing URD-016. Measured on rev-gender-and-number (u6, 20 words) with
  the whole unit known: the same 4 words
  (w-surkh, w-gulaabi, w-pyaazi, w-neela) are offered on every single call,
  and the other 16 never appear via this fallback under any circumstance.
  On rev-the-wider-world (u39, 117 words) only 19 (16%) can ever surface
  this way. Pre-existing behavior (seeded, not random, content selection is
  this project's deliberate convention — see `lib/shuffle.ts`'s own
  docstring) made newly visible, not newly broken, by URD-016 shrinking the
  pool a fallback draws from down to a single unit's dozen-to-hundred words,
  where a shuffle quirk that barely mattered against a course-wide pool of
  thousands now determines the entire fallback's content. Not blocking.

  RESOLVED. `reviewWordPool`/`reviewLetterPool` (`src/lib/review.ts`)
  gained a `visit = 0` parameter, folded straight into `prioritizedPool`'s
  shuffle seed (`` `${lessonId}:words:${visit}` `` /
  `` `${lessonId}:letters:${visit}` ``, tier index still appended after it)
  — so the per-tier shuffle now varies across replays instead of being
  fixed by `lessonId` alone, while staying deterministic for any one visit
  (replaying identical state reproduces the identical slice, not fresh
  randomness on every render). Threaded through `fallbackReviewRefs` and
  `buildLessonExercises` (`src/exercises/generator.ts`), both call sites
  updated.

  The "something that actually changes between visits" the item asked for
  is `completedLessons[lesson.id]?.done` — a real counter already living in
  the progress store (`useProgressStore.ts`'s `finishLesson`), incremented
  by exactly 1 on every completion of that lesson, not new state invented
  for this fix. `LessonScreen.tsx` reads it via `useProgressStore.getState()`
  inside the same `useMemo` that already reads `srs`/`srsType`/`known` the
  identical non-reactive way, and passes it through as `visit`.

  Verified beyond the two required unit tests: simulated 500 visits of
  `reviewWordPool` against rev-gender-and-number's fully-known 20-word unit
  and tallied how often each word surfaces in a 4-word slice — all 20
  appear, 85-118 times each (~17-24%, close to the uniform 20% expected) —
  the actual proof this reaches the 16 words the item named as permanently
  unreachable, not just that two arbitrary visit numbers happen to differ.
  Also confirmed the fix only ever changes shuffle *order*, never exercise
  *count*, so every existing caller that omits `visit` (`achievements.ts`,
  every `scripts/check-*.js` check) gets the byte-identical default-0
  behavior its existing numbers already depend on — reran each to confirm.

  New tests in `src/lib/review.test.ts`: two different visits of the same
  fully-known unit produce different word slices; the same visit reproduces
  the identical slice; omitting `visit` matches `visit=0` explicitly
  (back-compat); the letter side rotates too. Confirmed the fix is a real,
  catchable regression, not just plausible: reverted `review.ts`/
  `generator.ts` alone (keeping the new tests) and two of the four failed
  with the exact shape the item names — the same w-surkh/w-gulaabi/
  w-pyaazi/w-neela slice on every call; restored and reconfirmed clean.

  THE CRITIC (only critic dispatched — pure engagement-pipeline fix, no new
  teaching content or screen change): no BLOCKING, no MAJOR. Traced
  `RootNavigator.tsx` directly to confirm `Lesson` is pushed fresh on every
  real entry (never the already-focused route), so the visit count genuinely
  updates between mounts rather than only in theory; independently ran the
  500-visit frequency check above; independently reran every caller of
  `buildLessonExercises` to confirm none needed the new parameter. One
  MINOR: the letter-rotation test's fixed 0-vs-5 visit pair happened to pass
  today but could coincidentally collide for other pairs given only 4
  letters to permute (visits 4/5/6 all land on the same ordering) — rewritten
  to sample 20 visits and assert the orderings aren't all collapsed to one,
  independent of which specific pair differs.

  `npx tsc --noEmit` / `npm run lint` / `npm run format:check`: clean.
  `npx vitest run`: 187/187 (183 + 4 new). `npm run check:all`: all 30
  steps pass against a deploy-shaped build.
