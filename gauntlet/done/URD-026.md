## URD-026 — Sentences drill grammar the learner hasn't been taught yet, now 3x
attempts: 0
files: src/exercises/generator.ts (readableSentences), src/data/units.ts
definition of done: `readableSentences` filters a sentence pool on
  individual word forms only — it has no notion of whether the
  *grammatical construction* a sentence illustrates (future tense,
  ability, obligation, comparative...) has been taught by a `G()` lesson
  yet. Measured against the real path order: `s-intermediate` (units.ts,
  an elementary-unit lesson) draws sentences tagging g-future/g-ability/
  g-obligation/g-comparative in 6 of 8 picks, all taught 90-115
  lesson-positions later; `s-intermediate-2` draws 8 of 8 untaught. Give
  `readableSentences` (or `S()`) a concept-readiness filter mirroring how
  word-level readiness already works, or move the two offending lessons
  later in the path so their prerequisite concepts are already taught.
verify: a script cross-referencing each sentences lesson's drawn sentence
  ids against ALL_LESSONS grammar-teaching order reports 0 lessons with a
  drawn sentence whose tagged concept is taught later in the path.
notes: Found by CURRICULUM CRITIC reviewing 0453fa7. Pre-existing — the
  word-only filter and the two lessons' placement both predate this
  commit — but this commit is what first pipes that exposure into SRS
  (via meaningPick/wordFromMeaning's `gradeItem` calls) and triples how
  often an under-taught construction is shown in one sitting. Same shape
  as the letters SRS-defeat bug fixed generically this session
  (src/lib/sessionGrading.ts), applied to grammar readiness instead of
  scheduling — an existing gap this project has already been burned by
  once, now amplified by a length change rather than caused by one.

  RESOLVED: a new `taughtConceptsUpTo(lessonId)` function (`src/lib/
  review.ts`) walks the path in order, the same pattern `taughtUpTo`
  already uses for letters/words, collecting which grammar concepts a
  `G()` lesson has taught by a given position — inclusive of that lesson's
  own concept (load-bearing, not cosmetic: THE CRITIC confirmed by direct
  measurement that excluding a lesson's own concept would empty every one
  of the 25 grammar concepts' own reinforcement pool, triggering
  `readableSentences`' fallback-to-unfiltered-pool for all of them,
  reintroducing the exact bug this item fixes through the back door).
  `readableSentences` (`generator.ts`) now drops any sentence whose
  `.concept` isn't yet in that set, alongside its existing word-readiness
  filter; a sentence with no `concept` at all is left alone, same as an
  untaught-by-anyone word form.

  Verified against the real course, not the item's own two named examples
  alone: reverting the concept filter reproduces 41 violating picks across
  9 of the 12 real "sentences" lessons (not 2) — g-postpositions,
  g-oblique, g-obligation, g-dative, g-future, g-comparative, g-ability,
  g-subjunctive, g-relative, g-passive, g-perfect, g-compound and
  g-causative all appear before their own teaching lesson somewhere in the
  unfixed course. Both THE CRITIC and CURRICULUM CRITIC independently
  reproduced this exact count (41) by different methods (reverting the
  code vs. reimplementing the filter from scratch).

  A new check-order.js section (URD-026, search for `conceptFindings`)
  drives the real `buildLessonExercises` output — not a reimplementation
  of `readableSentences`, to avoid the class of bug where a check shares
  the logic it exists to catch — for every real "sentences"/"grammar"
  lesson, both tracks, and flags any exercise whose sentence tags an
  untaught concept. This is wired into `npm run check:order`, which check:all
  already runs (unlike check:shape, this is a hard CI gate, not a
  documented-but-unwired tool). 0 findings on the real course.

  THE CRITIC (mandatory) found no BLOCKING issues: verified the inclusive-
  self semantics are necessary (above), verified the combined word+concept
  filter never hits its unfiltered-pool fallback for any real lesson (0 of
  12), verified the sentence-recovery lookup in check-order.js
  (`SENTENCES.find` by word id) cannot false-match across `WORDS`/
  `PHRASE_WORDS`/`SENTENCES`' disjoint id namespaces, confirmed `check:order`
  passes clean, and confirmed via induced failure that reverting the fix
  reproduces exactly 41 findings before the fix was restored and
  reconfirmed clean. Also confirmed `src/data/units.ts` correctly did NOT
  need touching: `check:shape --kind=sentences`/`--kind=grammar` show
  identical results with the fix reverted or applied (only the
  pre-existing, separately-tracked g-plurals residual, URD-029, unaffected
  either way) — the generator-side fix causes zero new length shortfalls.

  CURRICULUM CRITIC (no MAJOR/MINOR blocking findings) independently
  reproduced the same 41-violation count via a from-scratch
  reimplementation (cross-checked against THE CRITIC's revert-based count)
  and additionally: measured post-filter candidate-pool sizes across all
  12 sentences lessons (17-69 candidates against an 8-sentence draw, 2.1x
  headroom at the thinnest, no risk of within-lesson repetition); pushed
  back on the item's own framing that repositioning "the two offending
  lessons" was ever a viable alternative to the generator-side fix, given
  9 of 12 lessons violated pre-fix, not 2 — worth recording explicitly so a
  future reader doesn't read the DoD's "or move the two lessons" as a
  live, declined option at the problem's real scale; and flagged that
  URD-027 (68% of the sentence pool unreached) needs a footnote, not a
  regression report — the reachable set's aggregate size is unchanged
  (81/256) but its *membership* churned by 41 sentences, so URD-027 should
  re-measure rather than reuse cached reachable-id lists. Added to
  URD-027's own queue entry.

  Full `check:all` (26 steps, including the now-updated `check:order`)
  passes. New unit tests (`src/lib/review.test.ts`,
  `describe('URD-026: taughtConceptsUpTo'`) cover the pure logic
  (exclusivity of untaught concepts, inclusive-self, monotonic
  accumulation along the real path, and the unplaced-lesson-id fallback);
  the whole-system content-wiring guarantee is check-order.js's job per
  this project's "two kinds of test, no overlap" convention, not
  duplicated as a second, redundant vitest assertion.
