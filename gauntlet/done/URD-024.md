## URD-024 — A sentences lesson is not one exercise per sentence

attempts: 0
files: src/exercises/generator.ts, src/data/units.ts
definition of done: A sentences lesson gives each sentence a
  meet-recall-produce climb, matching what vocab, letters and phrases
  already do, instead of showing it once and moving on. Today `S()`
  produces `lesson.size` exercises for `lesson.size` sentences — 5 (later
  8) exercises, 0.8-1.3 minutes, an interruption rather than a sitting per
  check:shape's 3-8 minute band. `check:shape` reports this as part of
  URD-A02's remaining backlog (sentences row: 12 short lessons).
verify: npm run check:shape
notes: The fourth and last row of URD-A02's remaining backlog after
  URD-010 (review), URD-013 (letters) and URD-012 (phrases share/run).
  The same reshape-into-Word-shaped-objects move `PHRASE_WORDS` made for
  phrases applies here (`SENTENCE_WORDS`), so it flows through the
  existing meaningPick/wordFromMeaning components rather than needing new
  UI. Read gauntlet/LEDGER.md's URD-013 entry before touching this — the
  pattern that cost that item four critique rounds (verify one property,
  ship, have review find a different property broke) is the thing not to
  repeat.
