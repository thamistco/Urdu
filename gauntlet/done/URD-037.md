## URD-037 — check:path has no floor, so a real regression could read as a pass
attempts: 1
files: scripts/check-path.js
definition of done: `check:path` asserts only an upper bound (`n` mounted
  rows over `BOUND` fails); nothing asserts a *lower* bound, so a scenario
  that mounts zero rows — the accordion silently failing to render anything
  — passes exactly as cleanly as one mounting the expected 81 or 94. Add a
  floor per scenario (e.g. "fresh guest" should mount at least the lessons
  in an open level's own unit, not zero) so a genuine render failure fails
  loudly instead of reading as an excellent bound.
verify: temporarily force the accordion's `isOpen(lvl)` gate to always
  `false` (mounting nothing) and confirm check:path now fails; today it
  would report "0 lesson rows mounted (bound: 114)" and exit 0.
notes: Found incidentally verifying URD-006 — `npm run check:all`, run
  twice back to back, reported a different scenario mounting 0 rows each
  time ("learner deep into the course: 0" once, "fresh guest: 0" the other),
  while `npm run check:path` run alone, three times in a row, reported the
  normal 81/94/94 every time. So this is a real flake under check:all's
  full-sequence load (not reproduced standalone, not investigated further
  here — a timing race between the build and the check, or system load from
  running 25 steps back to back, is the likely cause) that the check's own
  bound cannot catch either way, since "0" is a legal reading. Unrelated to
  URD-006's own change (no file this item touched is anywhere near
  `check-path.js` or `HomeScreen.tsx`'s accordion).

  RESOLVED. Added `expectedOpenLevel(completedLessons, skippedLessons)`,
  mirroring `HomeScreen.tsx`'s own `currentId`/`currentUnit`/`currentLevel`
  derivation exactly (first lesson in path order not completed or skipped,
  then that lesson's own unit's level; `LEVEL_ORDER[0]` for a fresh guest)
  rather than assuming which level a scenario lands on — a hand-guessed
  floor would drift the moment the course's own length or level boundaries
  moved, the same failure mode this file's own `BOUND` cap already guards
  against for the upper bound. Confirmed the mirror is real, not assumed:
  `HomeScreen.tsx`'s `order` comes from `unitsForTrack(track)`, which
  returns `UNITS` unchanged for any track but `'roman'` — and every
  scenario here uses the guest default track (`'both'`), never sets it to
  `'roman'` — so `order` and `ALL_LESSONS` are the same array by id for
  every scenario this file actually runs.

  Each of the three scenarios (fresh guest, learner deep into the course,
  tapping every level's header in turn) now has a floor computed from real
  content: the exact expected level's own lesson count for the first two,
  and `maxLevelLessons` itself (already computed for the ceiling) for the
  third, since tapping through every level should reach the largest one at
  some point.

  Live-verified exactly per this item's own verify command: temporarily
  forced `HomeScreen.tsx`'s `isOpen` to always return `false`, rebuilt,
  reran — the check now reports 3 failing floors with real exit code 1
  (checked directly, not through a pipe, after an early draft of this
  verification mistakenly read a piped `tail`'s exit code instead of
  `node`'s own). Reverted (`git diff src/screens/HomeScreen.tsx` empty)
  and reconfirmed clean, exit 0, against the real numbers (81/95/95).

  A floor doesn't diagnose the flake this item's own notes describe as
  unresolved — that investigation is still open, and still out of this
  file's own reach either way, since the flake reportedly only reproduces
  under `check:all`'s full-sequence load, not standalone. What changes is
  the *consequence*: if that flake recurs, it now fails loudly with a
  named floor and level instead of quietly logging "0" and exiting 0,
  which is exactly what this item asked for.

  THE CRITIC dispatched (pure tooling-correctness fix, matching URD-004's
  own precedent for this shape of change — no lesson/word/screen/exercise-
  behavior changed). No BLOCKING, no MAJOR. Independently recomputed every
  number from `src/data/units.ts` directly (not trusting the script's own
  printed output) and got an exact match (beginner 81, elementary 84,
  intermediate 95, advanced 90); independently reproduced the induced-
  failure test end to end, including reverifying the revert; reran the
  click-driven "tapping every level" scenario 3 times back to back for
  flakiness (stable). Three MINOR, all fixed: a comment claiming
  `expectedOpenLevel` checked `skippedLessons` when the first draft's code
  didn't (now does — inert either way today, since no scenario here or in
  `enterAsGuest` ever populates it, and the direction of the gap was the
  safe one, a floor computed too low rather than too high); a duplicated
  inline computation of `maxLevelLessons` that could reuse the new
  `lessonCountForLevel` helper directly (now does); and a stale "81 or 94"
  in a new comment where the real second number, confirmed by the script's
  own current output, is 95.

  `npx tsc --noEmit`, `npm run lint`, `npm run format:check`: clean.
  `npx vitest run`: 180/180, unchanged — no test touches this script.
