## URD-014 — A wiped profile deserves a different sentence
attempts: 0
files: src/store/useProgressStore.ts, src/lib/progress.ts, src/lib/progress.test.ts
definition of done: A profile persisted at version 0 or 1 has its
  `completedLessons` and `skippedLessons` emptied by the v1 to v2 migration,
  because positional lesson ids could not be translated. That learner genuinely
  did lose their ticks, and the path-moved notice says nothing to them, because
  the migration has already removed the evidence that they had any. Either the
  migration records what it dropped, or a second notice covers the case. A test
  asserts a v1 profile with completions is told something.
verify: npm test -- src/lib/progress.test.ts
notes: Found by THE CRITIC on URD-003 as MINOR. The wipe is pre-existing and
  correct — the old ids really do not say which lesson they meant — but "we threw
  your ticks away and said nothing" is a worse silence than the one URD-003 just
  fixed, not a better one.

  RESOLVED: extracted the inline `migrate` arrow (`useProgressStore.ts`,
  only reachable through zustand's persist middleware) into a standalone,
  exported `migrateProgress(persisted, from)` in `lib/progress.ts`, testable
  directly. For the v0/v1 → v2 wipe branch, it now records whether there
  was anything to lose — `ticksWipedByMigration: hadTicks` — computed
  before the wipe, true only for a profile that genuinely had a completed
  or skipped lesson (matching `needsPathMoveNotice`'s own "nothing ticked,
  nothing to say" principle). A new, separately-worded notice card on
  `HomeScreen.tsx` shows when this flag is set: "Your lesson progress
  wasn't carried over," with copy that (unlike the sibling "Lessons were
  regrouped" notice) does not claim nothing was lost, because for this
  learner something genuinely was.

  THE CRITIC's first pass returned BLOCKING: the screen's mount-time
  auto-scroll effect only guarded on the pre-existing path-moved notice,
  not the new flag, so the new notice was reliably scrolled off-screen
  within ~1.5s for exactly the learners it exists to reach — reproduced
  live before being fixed. Also MAJOR: the two dismiss-once notices could
  stack in a reachable sequence. Fixed by splitting `pathNotice` (gated,
  rendered) from `rawPathNotice` (the raw computation) so the notices
  queue one at a time — and, caught only by live testing during that same
  fix, a second bug in the fix's own first draft: the `notePathSize`
  auto-record effect read the *gated* value, which would have silently
  erased the path-moved notice's own evidence the moment the wipe notice
  merely appeared, the identical class of silent loss this item exists to
  close, one layer up. Fixed by pointing that effect at the raw value
  instead. Also fixed two MINORs from that round: the notice's copy
  overclaimed "finished lessons" for what could be a skip-only wipe
  (retitled, reworded).

  A second, independent THE CRITIC pass re-reviewed the fixes rather than
  trusting the first round's own resolution — PASS, no BLOCKING or MAJOR
  remaining, one more soft MINOR (a residual word choice, "checkmarks"),
  fixed inline. DESIGN CRITIC measured contrast and tap targets directly
  (7.45:1 body, 11.99:1 heading, 100×51px button) rather than eyeballing,
  found no blocking issues, and flagged the same stacking root cause THE
  CRITIC had already found and which was fixed and re-verified by then.

  Verified throughout via disposable Playwright scripts (not committed):
  a real v1 profile with positional-id ticks, migrated for real through
  the app's own persist middleware, confirmed the notice renders, survives
  past the old auto-scroll failure window, and — when a second, real
  path-move condition is also present — shows one card at a time in the
  correct order, with no evidence lost in between.
