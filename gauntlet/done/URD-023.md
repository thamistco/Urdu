## URD-023 — Guarantee a phrases lesson has enough typeable phrases, not just reassign after drawing
attempts: 0
files: src/exercises/generator.ts
definition of done: The phrases branch draws 6 phrases uniformly from all 28
  with no floor on how many are typeable, then assigns exercise kinds among
  whatever it drew. When fewer than 2 of the 6 are typeable — 8.24% of draws,
  computed exactly (hypergeometric, 14 of 28 typeable, 6 drawn) — no
  reassignment can keep any kind under check:shape's 40% share, which is a
  fact about dividing six things three ways with two producible, not
  something the assignment logic can fix after the fact. The lesson that
  ships today draws 3 typeable and clears it, but that is this draw's luck.
  Fix it at the draw, not the reassignment: bias `seededShuffle`'s pick so at
  least `produceCount` of the drawn phrases are typeable, then fill the rest
  freely. A test asserts every one of many synthetic lesson ids at size 6
  clears the 40% share floor, not just the one that ships.
verify: npm run check:shape -- --kind=phrases
notes: Found by THE CRITIC reviewing URD-012, across two review rounds. The
  first round's fix (target-based reassignment instead of greedy) closed the
  ordering-dependence half of the problem; this is the other half, and
  cheaper than the two alternatives named when it was first found — a fourth
  exercise kind that doesn't depend on typeability, or growing the lesson
  size so the law of large numbers does the work. check:shape does not catch
  this today only because the one real lesson happens not to trigger it;
  it would catch a future one before it shipped, which is why this is MAJOR
  and not BLOCKING.

  RESOLVED: the draw itself now guarantees `produceCount` typeable phrases,
  computed against the target lesson size and the whole 28-phrase pool's
  eligible count (not the post-draw one). `eligiblePool` (typeable, not a
  fill-in-the-blank template) is drawn from first via
  `seededShuffle(eligiblePool, \`${lesson.id}:produce\`)`, sliced to
  `produceCount`; the rest of the lesson is filled from everything left
  over via a second seeded shuffle (`lesson.id`); the two groups are merged
  and shuffled once more (`\`${lesson.id}:order\`) so produce picks aren't
  clustered at the front, which would otherwise desync the `meet`/`recall`
  alternation counter downstream. Three independent seed keys, still fully
  deterministic per lesson id (confirmed against `seededShuffle`'s pure
  mulberry32-on-FNV-1a-hash implementation — no `Math.random`).

  Verified directly: 2,000 synthetic lesson ids at size 6, zero draws with
  fewer than 2 typeable phrases, zero single-kind shares over 40% (measured,
  matching the exact 8.24% hypergeometric failure rate the item computed
  for the old logic). Three new tests added to `generator.test.ts`
  (`describe('URD-023: ...')`): 500 synthetic draws never exceed 40% share,
  500 synthetic draws always land ≥2 typeable, and the real shipped
  "Everyday phrases" lesson clears the floor directly.

  THE CRITIC (mandatory) found no BLOCKING issues. Verified by hand and by
  script that the share-floor guarantee holds at every lesson size `P()`
  (units.ts) would ever accept (3, 5, 6, 8, 9, ... — sizes 1, 2, 4, 7 are
  rejected there already), confirmed the arithmetic never underflows or
  duplicates a phrase, confirmed determinism against `seededShuffle`'s real
  implementation, and independently recomputed the 8.24% hypergeometric
  figure to confirm the new tests would fail against the pre-fix code (they
  do — verified directly, see below). Flagged one non-blocking observation
  for curriculum judgment: biasing the draw changes which 6 of 28 phrases
  the one real shipped lesson teaches (measured: only 1 of 6 phrases
  survives the change), not just a reshuffling of the same set.

  CURRICULUM CRITIC reviewed that specific flag and found no MAJOR or MINOR
  issue: no project convention (docs/ENGINEERING_STANDARDS.md,
  CONTRIBUTING.md, or any check script) promises lesson content stability
  across a code change for ANY lesson kind — `seededShuffle`'s own contract
  is same-build stability (no reshuffle per visit), not
  stability-across-releases, and every other lesson kind already reshuffles
  under a content or algorithm edit the same way. `check:coverage` does not
  cover phrases at all, so no coverage invariant is affected. Considered and
  declined to file forward a "keep the old draw when it already clears the
  floor" refinement — real, low-cost, but benefits only the 91.76% of draws
  that didn't need fixing, on content with exactly one real lesson and no
  tracked user-progress-preservation concern in this project.

  check:shape --kind=phrases: zero run/share violations (the one problem it
  reports, "1 of 1 timed lessons under 3 minutes," is the pre-existing,
  separately-tracked URD-A02 length residual, not in scope here and not
  moved by this change). Full check:all (26 steps, including check:answerable,
  check:coverage, check:order, audit) passes. Verified via induced failure:
  reverting to the old single-draw-then-reassign logic reproduced exactly
  the predicted failure mode in both new tests (a synthetic draw landing 1
  typeable phrase, 50% share) before the fix was restored and reconfirmed
  clean.
