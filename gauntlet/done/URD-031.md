## URD-031 — A grammar lesson is not teach-plus-drills-and-stop

attempts: 0
files: src/exercises/generator.ts, src/data/units.ts
definition of done: A grammar lesson gives its concept a meet-recall-produce
  climb over its own tagged sentences, instead of teaching it, drilling it
  once per hand-authored drill (1-3, fixed by content), showing up to 2
  static sentences, and stopping. Today `G()` produces 2-6 exercises,
  0.3-0.9 minutes — an interruption rather than a sitting per check:shape's
  3-8 minute band — and `g-to-be` specifically is 50% grammarDrill, a
  check:shape run/share violation. `check:shape` reports this as the last
  unaddressed row of URD-A02's remaining backlog (grammar row: 25 short
  lessons).
verify: npm run check:shape -- --kind=grammar
notes: The fifth and last row of URD-A02's remaining backlog after
  URD-010 (review), URD-013 (letters), URD-012 (phrases share/run) and
  URD-024 (sentences length). Reuses the exact meet-recall-produce climb
  URD-024 built for sentences-kind lessons, applied to each concept's own
  tagged sentences (SENTENCES.filter(x => x.concept === c.id)) rather than
  a fresh mechanism — the drills stay as they are (there is no larger pool
  to draw more of; each is a specific fill-in-the-blank, not
  interchangeable content). Read gauntlet/LEDGER.md's URD-013 and URD-024
  entries before touching this again — the pattern that cost URD-013 four
  critique rounds (verify one property, ship, have review find a different
  property broke) is the thing not to repeat.
