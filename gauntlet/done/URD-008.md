## URD-008 — Logical direction properties in Urdu-bearing components
attempts: 0
files: src/components/, src/screens/, src/exercises/
definition of done: The 8 remaining physical direction properties (`ml-`, `mr-`,
  `pl-`, `pr-`, `marginLeft`, `paddingRight` and friends) in components that can
  render Urdu become logical equivalents. A source scan fails on any physical
  property reintroduced there.
verify: npm run check:all
notes: Small and already mostly done — `writingDirection` is handled in
  `Text.tsx`, `HomeScreen.tsx` and `PracticeScreen.tsx`. Write the guard in the
  style of `check:theme`, which already does this shape of source scan for
  colour. Last in the queue because it is the least likely to be felt.

  RESOLVED: found 22 occurrences (not 8 — the item's count was measured
  2026-08-07 and had drifted), across `src/components/Card.tsx`,
  `src/screens/HomeScreen.tsx`, `PracticeScreen.tsx`, `SettingsScreen.tsx`,
  `LeaderboardScreen.tsx`, `LetterLabScreen.tsx`, `LoginScreen.tsx`, and
  `src/exercises/GrammarExercises.tsx`, `SentenceReading.tsx`,
  `DialogueExercise.tsx`. Converted all of them to their React
  Native/NativeWind logical equivalents: `ml-`/`mr-`/`pl-`/`pr-` →
  `ms-`/`me-`/`ps-`/`pe-`, `border-l-`/`border-r-` → `border-s-`/`border-e-`,
  and the matching inline props (`marginLeft`, `borderLeftWidth`,
  `borderLeftColor`, `borderTopLeftRadius`, `borderTopRightRadius`) →
  their Start/End equivalents — including `DialogueExercise.tsx`'s chat-
  bubble "speech tail" corner, keyed off the same `isA` flag that already
  drives its `items-start`/`items-end` alignment. Confirmed the entire
  change is behaviorally a no-op today: nothing in the app anywhere sets an
  RTL container direction (`I18nManager`, CSS `direction`), so `ms-4` and
  `ml-4` render identical pixels — the difference is whether that stays
  true by construction going forward, or only because nobody has tried.

  New `scripts/check-direction.js` (npm script `check:direction`, wired
  into `.github/workflows/deploy-preview.yml` right after `check:theme` —
  `check:all` went from 25 to 26 steps), in the exact structural pattern
  `check:theme` established: source-scans `src/components/`,
  `src/screens/`, `src/exercises/` for the physical Tailwind classes and
  inline style keys, with a `check:direction-ok` line-marker escape hatch
  mirroring `check:theme-ok`. Deliberately out of scope, documented at the
  site: `left`/`right`/`top`/`bottom` absolute-positioning offsets (a
  different style domain, and at least one use — a notification badge
  pinned to a corner — may be a deliberate design choice, not a bug) and
  `textAlign: 'left'`/`'right'`/Tailwind `text-left`/`text-right` (React
  Native's `textAlign` has no `'start'`/`'end'` value to swap to, and
  `'auto'` is not a like-for-like substitute — it would change how the
  `writingDirection: 'ltr'`-pinned English copy near Urdu script behaves).

  THE CRITIC passed with no BLOCKING and one MAJOR, fixed same round: the
  check's `STYLE_PHYSICAL` regex only matched `name: value`, missing
  object-shorthand syntax (`{ marginLeft }`, no colon) — a live idiom
  already used in this codebase for a different prop (`{ color }` in
  `Stats.tsx`, `LessonComplete.tsx`) — and reproduced live: adding
  `{ marginLeft }` to `Card.tsx` left the check reporting clean. Also
  flagged a quoted-key variant with the same gap. Fixed by rewriting the
  regex to cover explicit, quoted and shorthand key forms; re-verified via
  induced failure for both the original gap and the fix. THE CRITIC also
  hand-traced all 22 conversions (including the one risky mapping,
  `DialogueExercise.tsx`'s bubble corner) and confirmed none were flipped,
  and agreed with the lead's two scope calls (including `LeaderboardScreen`
  despite it rendering no Urdu script itself).

  DESIGN CRITIC could not complete — two dispatches both hit the account's
  monthly spend limit mid-review, an infrastructure interruption rather
  than a finding, not retried a third time. In its place the lead drove the
  real built app directly (Playwright, the same machinery `check:stability`
  uses) and screenshotted the two highest-risk accent-stripe sites
  (`LetterLabScreen`'s note card, `LeaderboardScreen`'s per-row zone
  stripe) — both render identically to before the property rename. Not a
  substitute for a full DESIGN CRITIC pass; recorded plainly as an
  incomplete review rather than treated as a pass. No new queue items filed.
