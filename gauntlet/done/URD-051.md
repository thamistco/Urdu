## URD-051 — soak's --track flag never actually sets the app's learning track
attempts: 0
files: scripts/soak.js, scripts/lib/serve-dist.js
definition of done: `npm run soak -- --track roman` actually plays the Roman
  track — `enterAsGuest`'s injected `harf-settings` carries `track: 'roman'`,
  not the guest default (`'both'`), verified by reading the real app's
  `localStorage` back after entry, not by trusting the CLI flag's own name.
verify: a script (or an assertion added to soak.js itself, gated behind a
  flag so it isn't paid on every real run) that enters as a guest with
  `--track roman`, reads back `localStorage['harf-settings']` in the page,
  and confirms `state.track === 'roman'` reports true. Today it reports
  `'both'`.
notes: Found by THE CRITIC reviewing URD-035's fix, which cited `npm run
  soak -- --start 200 --track roman --seed 55 --lessons 3` as one of three
  independent reproductions of that item's crash. Verified directly: soak's
  `--track roman` branch calls `enterAsGuest(page, url, { goal: 'speak' })`
  (`scripts/soak.js`'s `TRACK === 'roman'` spread), and `enterAsGuest`
  (`scripts/lib/serve-dist.js`) unconditionally writes `harf-settings` with
  only `soundEnabled`/`hapticsEnabled`/`reducedMotion` — `goal` feeds an
  unrelated onboarding field, never `track`. Reading `localStorage` back
  after entry confirms `track` stays at the guest default, `'both'`, no
  matter what `--track` says. Every soak run this session that passed
  `--track roman` (including this item's own "roman-track" reproduction,
  and whichever earlier items believed they were exercising the Roman
  track specifically) was actually driving the `both` track under a
  misleading label — the exact "the workflow said success and it was
  wrong" pattern this project's own CLAUDE.md names as its first
  non-negotiable, now found inside shared test infrastructure rather than
  CI itself. Does not invalidate URD-035's fix (independently re-verified
  live and via a passing/reverting unit test, both against the real
  `both` track), but any future item that means to test Roman-track-
  specific behavior via `npm run soak -- --track roman` is not testing
  what its own invocation claims until this is fixed.

RESOLVED. `enterAsGuest` (`scripts/lib/serve-dist.js`) gained a new fourth
argument, `settings` (default `{}`), that merges into the `harf-settings`
write the same way the pre-existing third argument (`state`) already
merges into `harf-progress`'s — the two stores' overrides were being
confused for each other (`goal`, an onboarding field, was the only thing
`--track roman` ever actually set), and now have separate, named paths.
`soak.js` passes a new `trackSettingsFor(TRACK)` helper as that fourth
argument (`{}` for the guest default `'both'`, `{track: TRACK}` otherwise),
pulled into its own top-level function rather than an inline ternary — the
async run-loop IIFE was already at this file's own ESLint complexity
ceiling and a third inline ternary tipped it over.

New verification script `scripts/check-soak-track.js` (`npm run
check:soak-track`, not part of `check:all` — the item's own text says this
"isn't paid on every real run," matching `check:grammar-distractors`'
precedent for a real-but-non-gating check): drives the real exported
bundle, calls `enterAsGuest` with `--track roman`/`--track script`/no
override, and reads real `localStorage['harf-settings']` back for all
three. Revert-verified: reverting `enterAsGuest`'s fix reproduces the
original bug exactly (`roman`/`script` both report back `'both'`) and the
new check fails with that exact shape; restoring it passes clean.

Confirmed end-to-end, not just at the storage layer: a live `npm run soak
-- --track roman` (SOAK_DEBUG=1) completed real exercises with no crash,
and — corroborated independently by THE CRITIC's own live spot-check, same
seed, `--track roman` vs. the default `both` — the served content
genuinely differs by track: `roman` produced `multipleChoice`/
`meaningPick`/`wordFromMeaning`/`listenTap`/`typeWord`, no script/letter
content at all, while the default track on the identical seed produced
`letterTrace`/`letterForm`/`letterPick`, real Nastaliq tile-matching
content. `LessonScreen.tsx` reads `track` from `useSettingsStore` and it
feeds `buildLessonExercises`' `teachesScript = track !== 'roman'` and
several kind-level Roman-transliteration gates directly — this is not a
localStorage round-trip in isolation, it changes what a learner (or a
soak run) is actually shown.

Dispatched THE CRITIC alone — a test-infrastructure fix with no UI or
curriculum content touched, matching ROLES.md's "not every item needs all
four." Per this session's newly-added ROLES.md rule (written directly
after URD-050's shared-checkout contention), dispatched into its own
isolated worktree since its own method includes reverting a fix to prove
the check fails.

**THE CRITIC: PASS, no BLOCKING.** Confirmed independently: all 7
pre-existing `enterAsGuest` callers pass at most 3 positional args, so the
new 4th parameter's `{}` default changes nothing for any of them (checked
every call site directly). Built the real bundle and ran `check:soak-track`
itself; ran its own mutation test (reverted the fix, confirmed the check
fails with the exact predicted shape, restored it, confirmed green again).
Read `useSettingsStore.ts`'s `persist` config directly and confirmed no
`partialize`/custom `merge` and that the existing `migrate` function only
ever adds fields via spread, never drops or transforms `track` — the same
rehydration mechanism the pre-existing `harf-progress` writes already
relied on, now confirmed for `harf-settings` too. Ran its own live
comparative soak spot-check (same seed, `roman` vs. default) as the
concrete proof described above.

Two MINORs, both explicitly out of this item's scope and filed forward
rather than fixed here: **URD-064** (`--track`'s CLI arg accepts any
string with no validation against the real three-value enum — pre-
existing, one layer up from what this item fixed) and an unrelated,
already-known solver gap in `letterPick` that surfaced incidentally during
the live spot-check (not connected to this diff at all — occurred under
the default track, not the roman path under test).

**Process finding, folded back into ROLES.md immediately**: the isolated
worktree THE CRITIC ran in only carries committed history, so it could not
see this branch's own uncommitted work-in-progress — it improvised a
working diff-and-copy approach that cost real turns rediscovering a
problem the dispatch prompt should have anticipated. Added a follow-up
amendment to the same ROLES.md rule this session already wrote (right
after URD-050), naming the fix for next time: paste the diff into the
dispatch prompt directly, or make a cleaned-up-afterward WIP commit before
dispatching.

Verified: `npx tsc --noEmit`, `npx vitest run` (245/245, unchanged — this
item touches no test-covered app code), `npm run lint`, `npm run
format:check`, `npm run check:soak-track` (all three cases), and a full
`npm run check:all` (30 steps) all clean on the final commit.
