## URD-049 — check:answerable is flaky: homograph pairs can land as two indistinguishable distractor options
attempts: 0
files: src/exercises/generator.ts (distractorsFor)
definition of done: `distractorsFor` (generator.ts, ~lines 432-456) dedupes
  candidate distractors by `.id`, `.cue`, and `.meaning`, but never by
  `.urdu` — so two distinct vocabulary entries that are genuine homographs
  (identical spelling, different meaning — e.g. `w-chashma`/`w-chashma2`,
  چشمہ meaning both "glasses" and "spring (water)") can both land in one
  `meaningPick`/`wordFromMeaning` option set, indistinguishable on screen
  from each other even though they're logically distinct options. Add an
  `.urdu`-uniqueness guard alongside the existing three. Measured: 40
  homograph groups exist across the 2,281-word vocabulary; `consider()`'s
  unseeded `Math.random()` shuffle means which pair collides, if any,
  differs run to run, so `npm run check:answerable` fails roughly 3 of 10
  runs today, each time citing a different, unrelated sentence/word id —
  a check that fails on the wrong signal 30% of the time is worse than one
  that never fails at all (this project's own non-negotiable #2).
verify: run `npm run check:answerable` 10 times in a row; 0 of 10 may
  report a "two options are the same word" finding.
notes: Found by THE CRITIC reviewing URD-048, while root-causing why
  `npm run check:all` failed at `check:answerable` on both of two
  consecutive runs, each citing a different sentence id (`s-143`, then
  `s-67`). Traced to a real `meaningPick` exercise for `s-104` whose
  distractor set included both `w-chashma` and `w-chashma2`. Confirmed via
  10 additional standalone `check:answerable` runs: ~3 of 10 fail, citing
  a different, unrelated id each time — the flakiness is real and
  reproducible, not a one-off. Entirely unrelated to URD-048's own diff
  (`distractorsFor` untouched by that item; reproduces via plain
  vocabulary words with no sentence lesson involved) — filed separately
  rather than folded into that item's fix. Not a duplicate of URD-013
  (already closed): that item's flakiness came from `letterExercise`'s own
  unseeded `Math.random()`, a different call site entirely. Until fixed,
  `check:all` cannot be trusted to exit 0 on a single run for ANY item —
  a real failure and a homograph-collision false alarm look identical from
  outside, so a re-run that goes green does not by itself prove a fix
  worked. Recommended priority: high, given every other item's own
  `check:all` verification is undermined until this is fixed.

  RESOLVED: `distractorsFor` now has an unconditional `usedUrdu` set,
  seeded with the target word's own spelling and checked in `consider()`
  right alongside the existing `.id` check — a candidate sharing `.urdu`
  with the target or an already-chosen distractor is skipped regardless
  of `distinctCue`/`distinctMeaning`, matching the item's own ask exactly
  ("alongside the existing three," unconditional). `distractorsFor` was
  exported (previously private) so it could be unit-tested directly, the
  same way `distractLetters` already is in this file.

  Verified directly rather than trusting the fix's plausibility: ran
  `npm run check:answerable` 20 standalone times with the fix in place —
  0 of 20 failed. Reverted just the `.urdu` guard and re-ran 15 times —
  reproduced a real failure (1 of 15, consistent with the original ~30%
  rate on a probabilistic check), confirming the fix is what's actually
  responsible, not coincidence.

  Two new unit tests added (`generator.test.ts`,
  `describe('URD-049: ...')`), both deliberately built to be deterministic
  rather than relying on an unseeded shuffle happening to surface a
  collision (the exact property that made the original bug flaky): one
  gives `distractorsFor` a single-candidate pool consisting of the
  target's own real homograph sibling, for every one of the corpus's 40
  real homograph groups; one gives it a two-candidate pool of synthetic
  decoy `Word` objects sharing a manufactured spelling, guaranteeing both
  are considered together regardless of shuffle order. Induced failure
  (temporarily disabling the new guard) reproduced both tests failing
  with a clear, specific message; restored and reconfirmed clean.

  THE CRITIC (mandatory) found no BLOCKING issues. Confirmed the
  `usedUrdu.add` placement (before the later `distinctCue`/`distinctMeaning`
  checks that can still reject a candidate) is a real but harmless
  conservative tradeoff, and — checked specifically, not assumed —
  identical to the pre-existing `usedCues`/`usedMeanings` pattern already
  in the file, not a new design mistake introduced here; confirmed
  covered by the function's own wide widen-to-`WORDS` fallback, with zero
  observed degradation across 15 real `check:answerable` runs. Confirmed
  no naming collision from the new export (three internal call sites,
  the test file, no shadowing). Independently reproduced the induced
  failure and the 15/15-clean confirmation itself, not just trusting the
  implementer's own numbers.

  Full `check:all` (27 steps) confirmed passing twice on the final tree.
