## URD-006 — A new learner cannot be locked out with no way back
attempts: 0
files: src/lib/gamification.ts, src/screens/LessonScreen.tsx, src/lib/gamification.test.ts
definition of done: Either the first refill is affordable, or the lockout screen
  says plainly how long the wait is and how many gems short you are. A profile
  starts with 20 gems, a refill costs 40 (`LessonScreen.tsx:262`), and a lesson
  pays 5 to 10. So a learner who loses five hearts in their first two lessons
  faces a disabled button and a 30 minute wait per heart with no explanation of
  either. A test asserts a fresh profile can always either refill or be told the
  wait.
verify: npm test -- src/lib/gamification.test.ts
notes: The button is correctly `disabled={gems < 40}`; the problem is that it
  does not look disabled and says nothing about the wait. Found by the soak,
  which sat tapping it until its step budget ran out.


  RE-MEASURED after URD-A02 attempt 1: gems earned across the course roughly
  halve, while HEARTS_MAX stays 5 and a vocabulary lesson is now up to 43
  exercises. Five hearts against 43 questions is a tighter constraint than five
  against 9, and the refill is now further out of reach, so this is worse than
  when it was first measured.

  RESOLVED: added `REFILL_COST`, `gemsShortOfRefill(gems)` and
  `minutesUntilNextHeart(heartsUpdatedAt, now)` to `gamification.ts` (pure,
  unit-tested), wired a conditional message into the lockout screen — the
  unaffordable branch (the common case for a fresh profile) now reads
  "You're N gems short for a refill — the next heart arrives in about M
  minutes" instead of a one-size-fits-all sentence that never applied to
  the button sitting right below it. Also removed a duplicated hardcoded
  `40` in `useProgressStore.ts`'s `refillHearts` in favor of the shared
  `REFILL_COST`.

  THE CRITIC passed this with no BLOCKING findings but caught two real
  ones, fixed same round: the "fresh profile" test's `minutesUntilNextHeart`
  assertion was structurally unable to fail (clamped to a fixed range
  regardless of the arithmetic inside), fixed to assert the real expected
  value; and a doc comment claimed the lockout screen auto-dismisses once
  a heart regenerates, which the code does not do (only "Leave for now" or
  a successful refill clears it) — corrected. DESIGN CRITIC screenshotted
  the real screen at four gem/wait combinations, found no regression and a
  clear improvement, one MINOR (a contrast measurement dipping slightly
  under AA on one sampled line — pre-existing in this text-over-illustration
  treatment, not introduced here), and flagged (without fixing, correctly
  out of scope) that the disabled button's only cue is 40% opacity on an
  otherwise-unchanged gold pill — filed as URD-036. Also filed URD-037
  (check:path has no lower bound on mounted rows), found incidentally
  while re-verifying this item.
