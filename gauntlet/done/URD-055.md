## URD-055 — LessonScreen's three call sites into the SRS-grading wiring are untested
attempts: 0
files: src/screens/LessonScreen.tsx, src/screens/useSessionGradeFlush.ts
definition of done: URD-044 gave `useSessionGradeFlush`'s own ref/effect
  timing a real, mutation-tested suite — but the hook is exercised there
  only through a synthetic `Harness` that always calls `record`/`flushNow`
  correctly by construction. The three real call sites inside
  `LessonScreen.tsx` have zero automated coverage: the `useSessionGradeFlush(
  exercises, applyGrade)` call itself, `recordItemGrade(it, grade)` inside
  `onGraded`, and the explicit `flushPendingGrades()` inside `advance()` (the
  one whose own comment explains it exists so closing the app from the
  results screen doesn't silently lose SRS grading while keeping the
  rewards). If a future edit drops that `flushPendingGrades()` call from
  `advance()`, or drops `recordItemGrade` from `onGraded`'s body, nothing in
  `check:all` catches it — the exact "app is lying about what it graded" bug
  class URD-044 exists to guard against, one file over from where it used
  to live. A full rendered-`LessonScreen` test would need react-navigation/
  store/native-module mocking disproportionate to this (and would cut
  against this project's own "two kinds of test, no overlap" rule — neither
  bucket is "render one screen component") — so this likely wants a narrow
  seam: e.g. exporting `LessonScreen`'s body logic (not its JSX) as a
  testable function, or a thin fake-hook-return spy asserting the three call
  sites are actually reached with the right arguments during a scripted
  `onGraded`/`advance()` sequence, without rendering real UI.
verify: a test that fails if `flushPendingGrades()` is deleted from
  `advance()`, or if `recordItemGrade` is deleted from `onGraded`'s body —
  reverting either one makes the new test fail with a concrete, matching
  shape.
notes: Found by THE CRITIC reviewing URD-044. Not blocking — the doc
  comments URD-044 added are honest about testing the extracted hook, not
  "LessonScreen end-to-end," so this isn't a dishonesty finding, just a real
  remaining gap the ledger should name plainly rather than let readers
  assume URD-044 closed in full.

RESOLVED with a source-text seam rather than a component render or a
production-code refactor. New `src/screens/lessonScreenWiring.test.ts`
reads the real, shipped `LessonScreen.tsx` as text, strips comments (so a
commented-out call doesn't count — a real gap the item's own mutation
testing exposed in the first draft, see below), extracts the `advance`
and `onGraded` function bodies by brace-depth matching from a unique
anchor string, and asserts: `flushPendingGrades()` appears inside
`advance()`, specifically *before* `finishLesson(...)` (the ordering the
real comment above the real call names as the reason it's there, not
merely "somewhere in the block"); `recordItemGrade(` appears inside
`onGraded`; and the two names really are `useSessionGradeFlush`'s
`record`/`flushNow` aliases, not stale references to something else.

This is deliberately a third kind of test, neither a pure-logic unit test
nor a whole-system check-script — disclosed as such in its own doc
comment rather than pretending otherwise. The alternative (extracting
`advance`'s finish-sequencing and `onGraded`'s grade-recording into
small, dependency-injected pure functions, the same technique
`letterSpotGrading.ts`/URD-045 and `letterFormGrading.ts`/URD-054 already
used elsewhere in this exact codebase, and unit-testing those directly)
would have been more refactor-resistant, but is a real change to
production closures tightly coupled to several `useState` setters, not a
free extraction — judged disproportionate for guarding one wiring fact,
the same call the item's own text anticipated ("a full rendered test
would need mocking disproportionate to this").

Revert-verified five separate mutations against the real file, each
edited, tested, and restored in turn: deleting `flushPendingGrades()`
fails; *commenting it out* also fails (the specific gap a naive
`toContain` would miss); reordering it to after `finishLesson(...)` fails
on the ordering assertion specifically; deleting `recordItemGrade(it,
grade)` fails; commenting that out also fails. `git diff` empty after
each revert, confirmed before moving to the next.

Dispatched THE CRITIC alone (a test-infrastructure question about one
screen's wiring, no curriculum content, no new UI). Made a throwaway WIP
commit before dispatching and reset it after, per this session's ROLES.md
rule.

**THE CRITIC: no BLOCKING, no MAJOR.** Independently ran all five
mutations against the real file and got the same results each time,
confirmed the two anchor strings are unique in the file (no accidental
match against `onPress={advance}`/`onGraded={onGraded}`), and confirmed
`tsc`/`lint`/`format:check` and the full suite (259/259) all pass. Three
MINORs, all accepted rather than fixed:

1. The brace-depth scan is not string/template-literal-aware beyond the
   comment-strip pass — a future edit adding a literal `{`/`}` inside a
   string within `advance()`/`onGraded()` could silently miscount.
   Checked directly against the real file: no such string exists in
   either function body today, and the test's own doc comment already
   discloses this as a known trade rather than a hidden one.
2. `onGradedBody`'s length sanity-check margin (2037 of a 2500 threshold,
   ~19%) is the tighter of the two guards — real margin today, worth
   revisiting only if `onGraded` grows substantially.
3. The pure-function-extraction alternative described above would have
   been more refactor-resistant — a legitimate design alternative THE
   CRITIC named explicitly, while agreeing the chosen approach "is
   defensible, just not the only option" given the real production-code
   cost the alternative would carry.

Verified: `npx tsc --noEmit`, `npx vitest run` (259/259, 4 new tests),
`npm run lint`, `npm run format:check`, and a full `npm run check:all`
(30 steps) all clean on the final commit.
