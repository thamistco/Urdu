## URD-022 — Letters that look alike should not be drilled as if they don't
attempts: 0
files: src/exercises/generator.ts, src/data/letters.ts
definition of done: `l-3` teaches `daal`/`Daal` and `re`/`Re`/`ze`/`zhe` —
  dot-pairs distinguished only by a diacritic — with no ordering or
  weighting that references visual similarity anywhere in the pipeline
  (confirmed: no reference to confusability in letters.ts, units.ts or
  generator.ts). Drilling visually confusable letters back to back with
  identical weight risks teaching the confusion rather than resolving it.
  Either separate confusable pairs within a lesson's rounds, or give them
  extra sightings relative to visually distinctive letters in the same
  group.
verify: npm run check:shape -- --kind=letters
notes: Found by the CURRICULUM CRITIC reviewing URD-013. Recommended over
  URD-021 as the higher-priority pick if only one of the two is taken next,
  because it actively works against the letters just taught rather than
  merely under-using them.

  RESOLVED: added an optional `confusableWith?: string` field to `Letter`
  (`letters.ts`), populated on 16 letters that are dot/diacritic variants
  of a same-group base letter, derived from each letter's own curated
  `note` field describing a shared shape ("same bowl as", "X with N dots",
  "X with the retroflex mark") — never invented beyond what the note
  itself supports. A new `separateConfusables` (`generator.ts`) buckets a
  lesson's letters by `confusableWith ?? id` and places buckets
  largest-first into target positions taken evens-then-odds, which keeps
  every bucket's own members `positions.length` apart within a single
  pass — applied to reorder the letters-teaching branch's array before its
  existing round-major loop runs.

  CRITIQUE surfaced a real gap between what the first fix claimed and what
  it actually shipped, described in full below. Fixed: the verify command
  (`check-shape.js`'s new rule) now scans the lesson's full generated
  sequence and asserts an *exact*, computed count of forced adjacencies
  rather than a loose "some/none," and 3 more `confusableWith` pairs
  (`alif-madda`→`alif`, `noon-ghunna`→`noon`, `baRi-ye`→`choti-ye`) were
  added on top of the original 13.

  Measured on the final tree: every real letter lesson but one has zero
  confusable-adjacent exercises anywhere in its full generated output,
  including at round-transition boundaries. `l-3` (group 3, 7 letters) is
  the one exception — its largest bucket (`re`'s family, 4 letters) is
  exactly half the group rounded up, which forces one adjacency per round
  transition by a real combinatorial bound (proof and citation in
  `separateConfusables`'s doc comment); measured directly at exactly 5
  occurrences (`zhe` then `re`, once at each of the lesson's 5 round
  transitions), down from a measured pre-fix baseline of 30 (5 confusable
  pairs landing back to back within every one of the lesson's 6 rounds,
  under raw `letterIds` order) — real, substantial improvement, not
  perfect elimination, and now stated as such rather than overclaimed.

  Two new items filed forward, checked against QUEUE.md and done/ for
  duplicates first (none found): URD-046 (vary which specific pair
  collides at `l-3`'s forced wrap, instead of the identical `zhe`/`re`
  pair five times over) and URD-047 (CURRICULUM CRITIC's own suggestion —
  a discrimination exercise that poses a confusable pair directly against
  each other, which temporal separation alone does not provide).
