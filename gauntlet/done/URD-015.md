## URD-015 — Nothing should change under a finger that is already moving
attempts: 0
files: src/screens/HomeScreen.tsx, src/components/Reveal.tsx
definition of done: Dismissing the path-moved notice removes 275 px of content
  instantly, so the 23 px band at y 294 to 317 changes from "dismiss" to "start a
  lesson" between the press and the release. Give the card an exit, or hold the
  layout until the touch is over. A check drives the built app, taps Got it, and
  asserts nothing tappable occupies the released point within the tap window.
verify: npm run check:stability
notes: Found by the DESIGN CRITIC on URD-003, measured at +120ms and +1s: the
  card simply vanishes. Extend `check:stability`, which already owns the property
  that answering a question does not move it, rather than writing a new check.

  RESOLVED: `Reveal.tsx` gained optional `visible`/`onExited` props — passing
  `visible={false}` fades the card out in place (opacity only, no layout
  change, 300ms) and holds its full height until the fade genuinely finishes,
  at which point `onExited` tells the caller it's safe to stop rendering it.
  Provably inert for all ~49 other `<Reveal>` call sites, which never pass
  either prop. `HomeScreen.tsx`'s two notice cards gained local
  `showTicksWiped`/`showPathNotice` state that stays true through the exit
  fade even after the underlying store flag goes false, and the mount-time
  auto-scroll effect's guard now keys off those same flags so it stays
  suppressed for the whole time either card is visible, including mid-fade.

  `check:stability` gained `checkNoticeExit`: seeds a profile straight into
  the state that shows one specific notice (forcing a due-review card via
  `srs` so a real neighboring Pressable is guaranteed to sit where the
  notice's content ends), taps "Got it," and checks — via
  `document.elementFromPoint` at the exact original button coordinates,
  walking up to the nearest `[tabindex]` ancestor — that the same control (or
  nothing tappable) occupies that point immediately after the tap and 150ms
  later. Two real bugs were found and fixed in the check itself before either
  critic ran, both by deliberately reverting the real fix and watching the
  check not notice: `[role="button"]` detection matched nothing at all (this
  app's `Button` renders as a bare `<div tabindex="0">`, no explicit
  `accessibilityRole`), and the ticks-wiped scenario's seed didn't neutralize
  the sibling path-moved notice's own condition, so dismissing one correctly
  revealed the other queued behind it and the check mistook the new card's
  "Got it" for the old one still fading.

  THE CRITIC found BLOCKING: the reduced-motion exit path's `setTimeout` sat
  behind an effect whose dependency array included `onExited` (a fresh arrow
  function every `HomeScreen` render) and `reduced`. Any unrelated re-render
  during the 300ms exit window — a double-tap on the dismiss button, or the
  URD-014 stacking hand-off revealing a second notice mid-fade — reran the
  effect, and React's cleanup cancelled the pending timer with nothing left
  to reschedule it, permanently stranding a "dismissed" card on screen (and,
  as a consequence, permanently disabling the auto-scroll effect gated on the
  same flag) — reintroducing URD-014's "two notices visible at once" shape of
  bug via a new mechanism. Empirically confirmed against the real built app:
  a double tap left a card on screen past 4s, the stacking sequence left both
  cards on screen past 5s, both only under `reducedMotion: true` — a control
  run with `reducedMotion: false` (the real default) cleared correctly.
  Reachable by anyone using the app's own Reduce Motion setting. Also MAJOR:
  the check exercised only that same reduced-motion branch and never
  attempted the interaction that broke it.

  Fixed: the exit effect now reads `onExited`/`reduced` through a ref kept
  current by a separate, dependency-free effect, so its own dependency array
  is just `[visible, exit]` — only a genuine `visible` transition can start
  or cancel an exit. Added `checkNoticeSurvivesDoubleTap` to
  `check-stability.js`, which taps "Got it" twice in quick succession and
  asserts the card is actually gone 1.5s later. Verified via the same
  induced-failure discipline: reverted just the exit effect's dependency
  array, rebuilt, and the new check failed for both notices with "left the
  card on screen well past its exit window"; restored, reconfirmed clean. The
  stacking trigger was re-verified by hand against the restored fix (not
  automated, since the double-tap check already exercises the identical
  root-cause mechanism more cheaply): single tap, 1.5s wait, the path-moved
  notice it hands off to appears cleanly with exactly one "Got it" on screen.

  DESIGN CRITIC (never blocks): no blocking findings. Measured the held
  layout gap two ways (16px, identical, with and without a notice ever
  having shown) and tap targets (100×51px "Got it," well clear of platform
  floors) directly against the live app. Two MINOR notes: the exit's 300ms
  ease-in fade is a different motion shape than the app's ease-out entrance
  (a deliberate, justified asymmetry — can't translate away without either
  overflow or the exact jump this item exists to prevent); and a ~30-40ms
  window mid-fade where the text's effective contrast against its own
  darkening self-composite dips just under AA, inherent to any pure-opacity
  fade over a dark ground and past the point a learner who just dismissed it
  is still reading.
