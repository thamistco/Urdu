## URD-053 — se/seen/swaad, baRi-he/choti-he/do-chashmi-he and te/toe have the same undisambiguated-collision gap URD-038 fixed for ذ ز ض ظ
attempts: 0
files: src/data/letters.ts
definition of done: every letter in these three same-sound groups gets the
  same real disambiguation cue URD-038 gave the four z-sound letters — which
  one is the everyday/native default (if one is), and for the rest, an
  anchor to the word `LETTER_CONTEXT_WORD` (generator.ts) actually shows
  during that letter's own lesson, not the letter's own decorative `word`
  field.
verify: a test extending (or mirroring) `src/data/letters.test.ts`'s own
  pattern for every letter in these three groups — each note names the real
  pattern (not just "another of the N ways Urdu spells X") and, where the
  note cites an anchor word, that word matches `LETTER_CONTEXT_WORD.get(id)`
  exactly, the same two properties URD-038's test checks for the z-group.
notes: Found by CURRICULUM CRITIC reviewing URD-038, which explicitly
  scoped itself to "at least the ذ ز ض ظ group" per its own queue entry —
  left open there on purpose, not overlooked, but nothing tracked the rest
  until now. Today: `se` ("In Urdu it sounds exactly like seen, one of
  three letters spelling the same s") and `baRi-he` ("In Urdu it sounds the
  same as choṭī he, an ordinary h") name the collision with zero cue.
  `swaad` ("the spelling is inherited from Arabic") and `toe`/`te` ("In
  Urdu it sounds like te"/"the soft dental t") gesture at the real pattern
  but don't commit to an anchor word the way URD-038's fixed notes now do.
  Whether "everyday vs Arabic-loanword" holds for these groups the same way
  it measurably does for ذ ز ض ظ (verified directly there, not assumed) is
  itself unverified — check it against the real word corpus the way
  URD-038 did before writing anything, rather than assuming the same split
  applies.

RESOLVED, with one real correction to the item's own title. `do-chashmi-he`
does NOT belong in this fix: verified directly that its `sound` field is
`'h (aspirate)'`, a genuinely distinct phoneme (it aspirates the preceding
consonant — k→kh, b→bh), not a second spelling of plain h, and its own
note already correctly describes that role. Treating it as a third member
of the he-collision would have been inaccurate, not merely incomplete —
this was checked directly (`LETTERS.filter(l => l.sound === 'h')` returns
exactly `[baRi-he, choti-he]`), not assumed from the item's own title.

The real he-group collision is therefore two-way, matching the real s-group
(three-way: se/seen/swaad) and t-group (two-way: te/toe). Each group's
default letter (seen, choti-he, te) is now named as the one to reach for
by default in native and Persian vocabulary alike, and in most Arabic
loanwords too — mirroring `ze`'s exact framing from URD-038, including its
non-overclaim: real Arabic-root words (انسان/insaan, انصاف/insaaf; کتاب/
kitaab, وقت/waqt; شکریہ/shukriya, شہر/shahr) do use the default letter, so
none of the three new "default" notes say it's reserved for native
vocabulary only. Each loanword-only letter (se, swaad, baRi-he, toe) is
anchored to the real word its own `LETTER_CONTEXT_WORD` entry shows during
the actual lesson, not a decorative field — verified by direct query, not
assumed:

```
se       -> متاثر (mutasir, "impressed")
swaad    -> صفر (sifar, "zero")
baRi-he  -> خدا حافظ (khuda haafiz, "goodbye")
toe      -> خط (khat, "letter")
```

The "everyday vs Arabic/Persian-loanword" split was verified against the
real corpus for all three groups before writing anything, the same way
URD-038 did — not assumed to transfer. Real counts
(`WORDS.filter(w => w.urdu.includes(glyph)).length`, 2,281 words total):
se 19, seen 373, swaad 83; baRi-he 106, choti-he 419; te 435, toe 53.
Every word sampled containing a loanword-only letter reads as Arabic-root
vocabulary. Unlike zaal/zwaad/zoe (18/39/29 words, genuinely rare), swaad
(83) and baRi-he (106) are not rare — their notes correctly omit the "a
handful of" framing the numbers don't support, saying only "never occur
outside Arabic/Persian loanwords" without a rarity claim.

Added a new `describe` block to `letters.test.ts` mirroring URD-038's own
four-test shape: group membership/sound-exclusivity, the `do-chashmi-he`
exclusion itself (asserting its sound is NOT `'h'`), the three defaults'
non-overclaiming "everyday" framing, and the four loanword letters'
Arabic-mention + anchor-word containment. Revert-verified all four
assertion shapes by hand (anchor-word removal ×2, `do-chashmi-he`'s sound
field, "everyday" removal from a default note) — each fails with the
exact predicted shape, restores clean.

Dispatched THE CRITIC and CURRICULUM CRITIC, each into its own isolated
worktree with the uncommitted diff pasted directly into the prompt (per
this session's ROLES.md rule). **Both dispatches failed on first attempt
with an account-wide API rate limit before doing any work** — retried
once each, and both retries succeeded.

**Both retries independently reported the identical BLOCKING finding, and
both were false alarms from the same cause.** Recreating the diff by hand
in their own worktrees from the prompt's pasted text, both critics found
the seven edited `note` strings used straight quotes/apostrophes
(`Urdu's`, `that's`) rather than this file's own curly-quote convention,
which — *if true of the real file* — would be an unescaped-apostrophe
syntax error inside a single-quoted string literal. Both critics
correctly flagged this as possibly a transcription artifact of how the
diff reached them as pasted prompt text rather than a defect in the real
uncommitted branch, and both explicitly told the lead to verify directly
against the real file before trusting the finding either way.

Verified directly, immediately: the real file on disk (edited throughout
via the Edit tool, never through the pasted-diff text either critic
worked from) uses proper Unicode curly quotes (`’ “ ”`) exactly as every
other note in the file does — confirmed via `grep`, and via `npx tsc
--noEmit` and `npx vitest run`, both clean, both run directly against the
real committed-branch file before and after this confirmation. The
BLOCKING finding is void: an artifact of pasting the diff into the
dispatch prompt (which flattened the file's real curly quotes to straight
ASCII ones in transit), not a defect in the codebase. Both critics'
instinct to hedge and name exactly how to verify it was the right call —
neither treated an unconfirmed suspicion as a confirmed defect, and
naming the check that would settle it made settling it immediate rather
than another round-trip.

Everything else both critics checked — corpus counts (exact match, both
independently re-derived them), sound-field exclusivity, anchor-word
correctness (all four verified against the real `LETTER_CONTEXT_WORD`,
not just the two each was asked to spot-check), the non-overclaim safety
check, the `do-chashmi-he` exclusion's linguistic merits, mutation testing
of all four new assertions — came back clean, independently, from both.

**CURRICULUM CRITIC, one real MINOR, filed forward as URD-067**:
`choti-he` and `do-chashmi-he` share a teaching group (the file's own
comment literally reads "the h family") and are taught back to back, yet
neither names the other via `confusableWith`, unlike `khe`, which links
to `baRi-he` despite being in a different group. This is a different axis
of potential confusion than URD-053 touches (visual shape, not sound) and
predates this item — `do-chashmi-he`'s own note already correctly explains
its real phonetic role independent of this gap. Not blocking.

Verified: `npx tsc --noEmit`, `npx vitest run` (249/249, 4 new tests), `npm
run lint`, `npm run format:check`, and a full `npm run check:all` (30
steps) all clean on the final commit.
